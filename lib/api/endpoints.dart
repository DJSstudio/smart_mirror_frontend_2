class ApiEndpoints {
  // Sessions
  static const sessionStart = "/session/start";
  static const sessionEnd = "/session/end";

  // Videos
  static const videoUpload = "/videos/upload";
  static const videoList = "/videos/list";
  static const videoDetails = "/videos";

  // Peer discovery
  static const peerSessions = "/peer/sessions";

  // Transfer
  static const transferRequest = "/transfer_session_request";
  static const transferComplete = "/transfer_session_complete";
}
